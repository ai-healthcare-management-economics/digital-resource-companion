document.addEventListener('DOMContentLoaded',()=>{const cards=[...document.querySelectorAll('.process-card')],q=document.getElementById('proc-q'),ch=document.getElementById('proc-ch'),typ=document.getElementById('proc-type'),count=document.getElementById('proc-count');[...new Set(cards.map(c=>c.dataset.chapter).filter(Boolean))].sort((a,b)=>+a-+b).forEach(v=>{const o=document.createElement('option');o.value=v;o.textContent='Chapter '+v;ch.appendChild(o)});function f(){const term=q.value.toLowerCase().trim();let n=0;cards.forEach(c=>{const ok=(!term||c.textContent.toLowerCase().includes(term))&&(!ch.value||c.dataset.chapter===ch.value)&&(!typ.value||c.dataset.type===typ.value);c.classList.toggle('hidden',!ok);if(ok)n++});count.textContent=n+' process maps';}[q,ch,typ].forEach(x=>x.addEventListener('input',f));f();});
