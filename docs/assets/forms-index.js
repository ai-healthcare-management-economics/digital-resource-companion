document.addEventListener('DOMContentLoaded',()=>{const cards=[...document.querySelectorAll('.form-card')],q=document.getElementById('form-q'),ch=document.getElementById('form-ch'),tier=document.getElementById('form-tier'),count=document.getElementById('form-count');[...new Set(cards.map(c=>c.dataset.chapter))].sort((a,b)=>+a-+b).forEach(v=>{const o=document.createElement('option');o.value=v;o.textContent='Chapter '+v;ch.appendChild(o)});function f(){const term=q.value.toLowerCase().trim();let n=0;cards.forEach(c=>{const ok=(!term||c.textContent.toLowerCase().includes(term))&&(!ch.value||c.dataset.chapter===ch.value)&&(!tier.value||c.dataset.tier===tier.value);c.classList.toggle('hidden',!ok);if(ok)n++});count.textContent=n+' browser forms';}[q,ch,tier].forEach(x=>x.addEventListener('input',f));f();});
